import React from 'react';
import { render, screen } from '@testing-library/react';
import { Add } from '@/math';

// Mocking Add function
const mockAddFunction = jest.fn();

describe('Home Page - Rendering', () => {
  it('hello', () => {});
});
